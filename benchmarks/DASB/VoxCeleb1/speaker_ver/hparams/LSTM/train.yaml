# ############################################################################
# Model: Speaker Verification/Identification
# Encoder: biLSTM+ Statistic Pooling
# Training: VoxCeleb1
# Authors:
#     - Pooneh Mousavi 2025
# ############################################################################
# Seed needs to be set at top of yaml, before objects with parameters are made
# Basic parameters
seed: 1986
__set_seed: !apply:torch.manual_seed [!ref <seed>]
output_folder: !ref results/VoxCeleb1/dac/<seed>
save_folder: !ref <output_folder>/save
train_log: !ref <output_folder>/train_log.txt
testing: True # If set to True, the test evlaution is done, otherwise skipped.

# Data files
data_folder: !PLACEHOLDER  # e.g. /path/to/Voxceleb
cached_data_folder: !PLACEHOLDER # e.g., path/to/cache

verification_file: https://www.robots.ox.ac.uk/~vgg/data/voxceleb/meta/veri_test2.txt #path/to/veri_test2.txt

train_data: !ref <cached_data_folder>/train.csv
enrol_data: !ref <cached_data_folder>/enrol.csv
test_data: !ref <cached_data_folder>/test.csv
dev_data: !ref  <cached_data_folder>/dev.csv

verif_batch_size: 2
n_train_snts: 300000 # used for normalization stats
skip_prep: False

do_verification: True

tokens_folder: !PLACEHOLDER  # Path to the folder where extracted tokens are saved.
pretrain_embeddings_folder: none  # Optional: If pretrain_embeddings is True, this should be set to the path where the pretrained embeddings are saved.

####################### Training Parameters ####################################
precision: fp32
number_of_epochs: 15
batch_size: 8
lr_model: 0.0001 # @orion_step1: --lr_model~"loguniform(0.00001, 0.1)"
base_lr: 0.00000001
max_lr: !ref <lr_model>
improvement_threshold: 0.0025
step_size: 65000
mask_length: 10
mask_prob: 0.65
lr_weights: 0.01
original_sample_rate: 16000
shuffle: True
random_chunk: True
sentence_len: 3

# Dataloader options
train_dataloader_opts:
    batch_size: !ref <batch_size>

enrol_dataloader_opts:
    batch_size: !ref <batch_size>

test_dataloader_opts:
    batch_size: !ref <batch_size>


ckpt_interval_minutes: 15 # save checkpoint every N min

####################### Model parameters ###########################
# Tokenizer parameters
# These parameters should be set according to the tokenizer used to extract tokens saved in <tokens_folder>.
vocab_size: 1024
num_codebooks: 2
sample_rate: 24000

# Feature parameters
encoder_dim: 1024
embedding_dim: !ref <encoder_dim> 
hidden_dim: !ref <embedding_dim> 
# If set to True, encoder_dim should match the dimension of the tokenizer. For Encodec, it is 128.
pretrain_embeddings: False
freeze_embedding: False
# if set to concat, you need to set embedding_dim to match the encoder_dim after concatenation. Eg, if you have 4 codebook, embedding_dim shoudl set to encoder_dim/4
embedding_strg: att_pool # option are concat and att_pool
scalar_embeddings: False

# Number of speakers
out_n_neurons: 1211  #1211 for vox1  # 5994 for vox2, 7205 for vox1+vox2
dnn_layers: 2 #  @orion_step1: --dnn_layers~"uniform(1, 4,discrete=True)"
dropout: 0.1
############################## models ################################
tokens_loader: !new:utils.tokens.TokensLoader
    data_path: !ref <tokens_folder>

discrete_embedding_layer: !new:model.custom_model.Discrete_EmbeddingLayer
   num_codebooks: !ref <num_codebooks>
   vocab_size: !ref <vocab_size>
   emb_dim: !ref <embedding_dim>
   hidden_dim: !ref <hidden_dim>
   freeze: !ref <freeze_embedding>
   init: !ref <pretrain_embeddings>
   scalar: !ref <scalar_embeddings>

attention_mlp: !new:model.custom_model.AttentionMLP
    input_dim: !ref <encoder_dim>
    hidden_dim: !ref <encoder_dim>

encoder: !new:speechbrain.nnet.containers.Sequential
   input_shape: [null, null, !ref <encoder_dim>]
   lstm: !new:speechbrain.nnet.RNN.LSTM
      input_size: !ref <encoder_dim>
      bidirectional: True
      hidden_size: !ref <encoder_dim>
      dropout: !ref <dropout>
      num_layers: !ref <dnn_layers>
   linear: !new:speechbrain.nnet.linear.Linear
      input_size: !ref <encoder_dim> * 2
      n_neurons: !ref <encoder_dim>

avg_pool: !new:speechbrain.nnet.pooling.StatisticsPooling
   return_std: False

classifier: !new:speechbrain.nnet.linear.Linear
   input_size: !ref <encoder_dim>
   n_neurons: !ref <out_n_neurons>
   bias: False


modules:
    encoder: !ref <encoder>
    classifier: !ref <classifier>
    attention_mlp: !ref <attention_mlp>
    discrete_embedding_layer: !ref <discrete_embedding_layer>


model: !new:torch.nn.ModuleList
    - [!ref <encoder>, !ref <classifier>, !ref <discrete_embedding_layer>, !ref <attention_mlp>]

####################### Decoding & optimiser ###########################
compute_cost: !new:speechbrain.nnet.losses.LogSoftmaxWrapper
    loss_fn: !new:speechbrain.nnet.losses.AdditiveAngularMargin
        margin: 0.2
        scale: 30

log_softmax: !new:torch.nn.LogSoftmax
    dim: -1

model_opt_class: !name:torch.optim.Adam
    lr: !ref <lr_model>
    weight_decay: 0.000002


# Schedulers
scheduler: !new:speechbrain.nnet.schedulers.CyclicLRScheduler
    base_lr: !ref <base_lr>
    max_lr: !ref <max_lr>
    step_size: !ref <step_size>
############################## Logging and Pretrainer ##########################

epoch_counter: !new:speechbrain.utils.epoch_loop.EpochCounter
    limit: !ref <number_of_epochs>

compute_error: !name:speechbrain.nnet.losses.classification_error

# Logging + checkpoints
train_logger: !new:speechbrain.utils.train_logger.FileTrainLogger
    save_file: !ref <train_log>

error_stats: !name:speechbrain.utils.metric_stats.MetricStats
    metric: !name:speechbrain.nnet.losses.classification_error
        reduction: batch

checkpointer: !new:speechbrain.utils.checkpoints.Checkpointer
    checkpoints_dir: !ref <save_folder>
    recoverables:
        model: !ref <model>
        scheduler: !ref <scheduler>
        counter: !ref <epoch_counter>
